# 07 — 3층 → 4층 계단 오르기 (MVP, 4층 맵 없이)

목표: 4층 맵을 만들기 전, 3층 사무실에서 출발해 계단 하단까지 Nav2로 이동한 뒤
`stair_traverse_node`로 계단을 올라 4층에 "도달"까지만 확인한다. 4층에서의
위치추정/맵 스위칭은 이 단계 범위 밖.

## 왜 Nav2/SLAM을 계단에서 끄는가

`go2_scan_maker`의 `base_footprint` 투영은 평지 보행 중 ±3~7° 흔들림을 보정하는
용도다. 실제 계단에서는 pitch가 20~30°+ 로 지속적으로 커지므로 이 보정 전제가
깨지고, 2D `/scan` 자체가 의미 없어진다 (계단은 2D 맵으로 표현 불가능한 지형).
따라서 계단 구간은 지도 밖의 "스크립트화된 구간"으로 분리해서 처리한다.

## 사전 준비

1. **계단 하단 웨이포인트 기록**: 3층 맵 위에서 로봇을 계단 앞(정면으로 계단을
   보는 자세)까지 조이스틱/텔레옵으로 이동시킨 뒤 `/amcl_pose`를 echo해서
   좌표를 저장해둔다. 이후 이 지점으로 `NavigateToPose`를 보낼 때 재사용.
2. **계단 실측**: 단수, 총 수평 이동거리(run), 경사각을 한 번 측정해서
   `stair_traverse_node`의 `max_duration_sec`, `min_climb_sec` 파라미터를
   현실적인 값으로 맞춘다. (예: forward_speed=0.25m/s, 총 수평거리 3m면
   최소 12초 소요 → min_climb_sec는 그보다 살짝 작게, max_duration_sec은
   여유 있게 40초 정도로 시작)
3. **odom 토픽 확인**: `stair_traverse_node`는 `/utlidar/robot_odom`
   (nav_msgs/Odometry)에서 IMU 쿼터니언을 읽어 pitch를 계산한다. 실제 로봇에서
   `ros2 topic echo /utlidar/robot_odom --once` 로 필드가 있는지 확인. 다른
   토픽/타입을 쓰고 있다면 노드 파라미터 `odom_topic`을 바꾸거나 콜백을 맞춘다.
4. **Request 메시지 확인**: `cmd_vel_bridge`가 이미 쓰고 있는 `unitree_api/msg/Request`
   필드 경로(`header.identity.api_id`, `parameter`)가 `stair_traverse_node.py`의
   `publish_api()`와 같은지 대조. 다르면 그 함수만 수정하면 된다.

## 계단 구조: 9단 → 착지참(유턴) → 5단

이 건물 계단은 단일 직선이 아니라 9단 오른 뒤 평지 착지참에서 180도 유턴,
다시 5단을 오르는 구조다. 단순히 "pitch가 평평해지면 끝"으로 보면 중간
착지참에서 오탐(조기 종료)이 나기 때문에, `stair_traverse_node` v2는 계단을
"구간(segment) 리스트"로 다룬다: climb(9) → turn(180°) → climb(5).

- climb 구간: pitch가 `pitch_climb_deg` 이상으로 올라갔다가 다시
  `pitch_level_deg` 이하로 `level_hold_sec` 이상 유지되면 종료. 단, 그 구간의
  최소 소요시간(`steps * seconds_per_step * min_climb_margin`)을 넘기 전에는
  종료 판정을 하지 않아서 착지참을 "다음 flight의 끝"으로 착각하지 않게 한다.
- turn 구간: 제자리에서 `Move(0,0,vyaw)`로 회전하며 odom의 yaw 변화량을
  누적, 목표 각도(`turn_angles_deg`, 허용오차 `turn_tolerance_deg`)에 도달하면
  정지하고 다음 climb 구간으로 넘어간다. 회전 방향은 `turn_direction`
  (+1 반시계 / -1 시계)으로 지정 — 실제 착지참에서 로봇이 어느 쪽으로
  꺾어야 하는지 확인 후 설정할 것.

## 실행 순서

```
# 1) 3층 맵 + Nav2가 이미 떠 있는 상태에서, 계단 하단 웨이포인트로 이동
ros2 action send_goal /navigate_to_pose nav2_msgs/action/NavigateToPose \
  "{pose: {header: {frame_id: map}, pose: {position: {x: <X>, y: <Y>}, orientation: {...}}}}"

# 2) 성공 확인 후, 별도 터미널에서 계단 전용 노드 실행 (9단-유턴-5단 예시)
ros2 run go2_nav_bridge stair_traverse_node --ros-args \
  -p flight_step_counts:="[9, 5]" \
  -p turn_angles_deg:="[180.0]" \
  -p turn_direction:=1.0 \
  -p seconds_per_step:=1.2 \
  -p forward_speed:=0.25 \
  -p turn_speed:=0.4 \
  -p max_duration_sec:=90.0
```

`seconds_per_step`은 반드시 실측할 것: `forward_speed`로 한 단 오르는 데 걸리는
평균 시간을 재서 넣으면, 9단/5단 각각의 최소 소요시간을 자동 계산해서 착지참
오탐을 줄여준다. 착지참에서 회전 반경(로봇이 유턴하기에 충분히 넓은지)도
사전에 실측/확인해둘 것 — 좁으면 `turn_speed`를 낮추고 필요시 회전 전 짧은
전진/후진으로 위치를 맞추는 구간을 추가해야 할 수 있다.

## 안전 체크리스트

- 첫 실행은 반드시 사람이 옆에서 지켜보며(추락/전복 대비) 저속으로 진행.
- Jetson Orin Nano 독 + LiDAR가 장착된 상태이므로, `AutoRecoverySet(true)`로
  자동 복구를 켜두면 넘어졌을 때 격하게 일어나다가 장비를 파손할 수 있다는 게
  공식 문서의 경고임. 페이로드가 있는 구성에서는 오히려 `AutoRecoverySet(false)`를
  검토하고, 대신 사람이 즉시 개입할 수 있는 거리에서 테스트할 것.
- `max_duration_sec` 타임아웃에 걸려 중간에 멈추는 경우를 대비해 항상 `Damp()`를
  즉시 호출할 수 있는 비상정지 스크립트/버튼을 준비해둘 것.

## 이번 단계에서 하지 않는 것

- 4층 맵 생성 (`slam_toolbox`로 4층 매핑) — 다음 단계.
- 계단 상단 도착 후 4층 맵으로 AMCL 재초기화 — 4층 맵이 있어야 가능.
- 완전 자동 트리거(펫치 기반 계단 자동 감지로 웨이포인트 없이 진입) — 현재 SDK
  상태 인터페이스에는 "전방 장애물/지형 종류" 필드가 없어서, 고정 웨이포인트
  기반 스크립트 방식이 현실적인 접근.
